- if a user uses Github oAuth, we'll create an Account containing github oAuth information and then create a name with a github n name, username and image.

- if a user uses Google oAuth, we'll create an Account containing google oAuth information and then create a name with a Google name, username, and image

- if a user uses github oAuth first and Google oAuth second and  then other later, we'll create that oAuth account and upfdate the user information to show the latest
oAuth name and image. the usernamer will stay as is  once it has been crated. It won't fluctuate
